[package]
name = "compile_with_bazel"
version = "0.0.0"

# Mandatory (or Cargo tooling is unhappy)
[lib]
path = "fake_lib.rs"

[dependencies]
log = "0.4.8"


[package.metadata.raze]
workspace_path = "//third_party/cargo"
targets = [
    "x86_64-unknown-linux-gnu",
]
genmode = "Remote"
gen_workspace_prefix = "interpreters"
package_aliases_dir = "cargo"
default_gen_buildrs = false

# Examples for using OpenSSL build by us
# from https://github.com/bazelbuild/rules_rust/blob/600b9c79bd2f96774ba2a6566405f331f5f46b60/examples/sys/complex/Cargo.toml
# and https://github.com/bazelbuild/rules_foreign_cc/issues/337#issuecomment-657004174
# [package.metadata.raze.crates.openssl-sys.'0.9.60']
# build_data_dependencies = ["@openssl"]
# additional_deps = ["@openssl"]
# buildrs_additional_environment_variables = { OPENSSL_DIR="../openssl/openssl" }
# 
# [package.metadata.raze.crates.libssh2-sys.'0.2.20']
# build_data_dependencies = ["@openssl"]
